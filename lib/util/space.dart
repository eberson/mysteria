import 'package:flutter/material.dart';

class Space {
  Space._();

  static EdgeInsets get stayBottom => const EdgeInsets.only(
        bottom: 30,
        left: 12,
        right: 12,
        top: 12,
      );
}
